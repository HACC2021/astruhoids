import React from 'react';
import CheckInForm from '../components/CheckInForm';

/** A simple static component to render some text for the CheckIn page. */
const CheckIn = () => (
  <CheckInForm />
);

export default CheckIn;
